import 'package:flutter/material.dart';

class CharacterCatalog extends StatelessWidget {
  const CharacterCatalog({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
